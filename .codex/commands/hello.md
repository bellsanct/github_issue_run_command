name: hello
description: Print a hello message
allowed_tools: Bash(echo), Read(*), Write
---

# hello

This command prints a greeting to verify that Codex commands are being detected and can run Bash.

What to do:
- Run a Bash echo to print a message.

```bash
echo "Hello from Codex command!"
```
